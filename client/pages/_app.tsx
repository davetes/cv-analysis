import type { AppProps } from 'next/app';
import Head from 'next/head';
import '../styles/globals.css';

export default function App({ Component, pageProps }: AppProps) {
  return (
    <>
      <Head>
        <title>HireAssist AI | Expert Technical Recruiting Assistant</title>
        <meta name="description" content="AI-powered technical recruitment assistant cross-referencing Job Descriptions, CVs, LinkedIn profiles, and GitHub code." />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Component {...pageProps} />
    </>
  );
}
